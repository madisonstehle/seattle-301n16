<html>

<head>
  <title>Star Wars People</title>
  <link rel="stylesheet" href="/css/reset.css" />
  <link rel="stylesheet" href="/css/style.css" />

  <script id="character_template" type= "text/x-handlebars-template">

    
    {{#each results}}
      <div class="character"> 
        <header>
          <h2>{{name}}</h2>
        </header>
        {{#if likes}}
        <span class="likeCount">{{likes}}</span>
        {{else}}
        <span class="likeCount">0</span>
        {{/if}}
        <button class="like">Upvote</button>
      </div>
    {{/each}}

  </script>

</head>

<body>
  <header>
    <h1>Star Wars Characters</h1>
    <p>Vote for your favorites to show what you like!</p>
  </header>
  <main>
    <section class="characters">

      <% for(let x = 0; x < results.length; x++) { %>
      <div class="character">
        <header>

          <h2><%= results[x].name %></h2>
        </header>
        <span class="likeCount"><%= results[x].likes || 0 %></span>
        <button class="like">Upvote</button>
      </div>
      <% } %>

    </section>

  </main>
  
    <section id="newCharacters">
      <div class="handlebarsHolder"></div> 
    </section>

    <button id="more">Get More Characters</button>

  <footer>
    <strong>&copy; 2020 Code Fellows</strong>
  </footer>

  <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.3/handlebars.min.js"></script>
  <script src="/js/app.js"></script>
</body>

</html>